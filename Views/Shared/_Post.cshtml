@model Product

               @*     <form asp-controller="Cart" asp-action="Post" method="post">
                        <div>
                        <label asp-for="Quantity" />
                        <input asp-for="Quantity" class="form-control" type="range"/>
                        <span asp-validation-for="Quantity" class="text-danger"></span>
                        <input asp-for="ProductId" type="hidden" value />

                        </div>
                        
                    </form>*@
                    <a class="btn btn-primary" 
                    asp-area="" 
                    asp-controller="Cart"
                    asp-action="Post"
                    asp-route-Quantity="1"
                    asp-route-ProductId="@Model.Id"
                    asp-route-UserName="@User.Identity.Name"
                    >Add to your Cart</a>
